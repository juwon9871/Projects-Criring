<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.clearing.database.bookLaundryMapper">
	<select id="selStoreData" parameterType="StoreUse" resultType="StoreUse">
		select * from tbl_store where LAT = #{LAT} and LNG =#{LNG}
	</select>
</mapper>